# Node
node_modules/
.next/
out/
dist/
next-env.d.ts
tsconfig.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*.egg-info/
.coverage
htmlcov/
.pytest_cache/

# Env / secrets
.env
.env.local
.env.*.local

# build artifacts
# 커밋 데이터는 슬림 폴백 data/portfolio.fallback.json (ADR-038) — server.json/캐시는 로컬 산출물.
# suggestions.json 은 gen:suggestions 가 prebuild 마다 서버 데이터에서 재생성 → 미커밋.
public/data/suggestions.json

# fallback sample 커밋 유지
!data/portfolio.sample.json

# Playwright
test-results/
playwright-report/
playwright/.cache/

# Vitest / coverage
coverage/

# bkit runtime
.bkit/

# Harness runtime artifacts — execute.py 가 자동 생성하며 git 커밋 대상 아님
phases/**/phase*-output.json
phases/**/step*-output.json
phases/**/step*-output-attempt*.json
phases/*/.lock
phases/*/run.log

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# Playwright MCP 스냅샷 아티팩트
.playwright-mcp/

# 에이전트 worktree (임베디드 repo)
.claude/worktrees/

# Vercel CLI 로컬 링크 (vercel link 산출물)
.vercel

# 대형 로컬 산출물 (ADR-038 — 커밋본은 슬림 portfolio.fallback.json)
data/portfolio.server.json
data/embeddings-cache.json
.lighthouseci

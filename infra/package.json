{
  "name": "portfolio-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AI Portfolio AWS infra (ADR-034) — Bedrock + S3 Vectors + Lambda ingestion, CDK TypeScript",
  "scripts": {
    "build": "tsc --noEmit",
    "synth": "cdk synth",
    "diff": "cdk diff",
    "deploy": "cdk deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "aws-cdk": "^2.1100.0",
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.220.0",
    "constructs": "^10.4.0"
  }
}
